week02
- linux commands
- ls, ls -a, ls -l, ls -al
  pwd, cd .git, cd .., touch week01.txt
  vi week01.txt, vi week02.txt
  echo "test" >> README.md
  cat README.md

- git commands
  git init
  git status
  git add week01.txt
  git config --global user.email "test@example.com"
  git config --global user.name "test"
  git commit -m "v0"
  git log
  git checkout hashcode
  git checkout -
  git branch -M main
  git remote add origin https://github.com/KyungBins/test.git
  git push -u origin main